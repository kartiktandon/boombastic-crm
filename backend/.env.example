# MongoDB Atlas connection
# Replace YOUR_DB_PASSWORD only in backend/.env or in Vercel, never in this file.
MONGO_URI=mongodb+srv://kartiktandon796_db_user:YOUR_DB_PASSWORD@cluster0.k1lzovp.mongodb.net/boombastic_crm?retryWrites=true&w=majority&appName=Cluster0
DB_NAME=boombastic_crm

# Use a long random value in production.
JWT_SECRET=REPLACE_WITH_A_LONG_RANDOM_SECRET

# Replace this with the deployed frontend URL in Vercel.
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500
